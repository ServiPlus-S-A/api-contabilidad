extends:
  - '@commitlint/config-conventional'

rules:
  header-max-length: [2, always, 72]
  type-enum:
    - 2
    - always
    - [feat, fix, refactor, chore, test, docs, style]
  type-case: [2, always, lower-case]
  type-empty: [2, never]
  subject-empty: [2, never]
  subject-full-stop: [2, never, '.']
